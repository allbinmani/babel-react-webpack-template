"use strict";

/** Application entry point, for example instantiate and start your app here */

/** Example:

import App from './app';

window.App = new App(document.getElementById('app-main'));

window.App.start()
    .then((app) => {
            console.log("App started", app);
    });

*/
